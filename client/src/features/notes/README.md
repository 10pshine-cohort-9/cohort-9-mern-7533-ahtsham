# `features/notes/`

Note queries and mutations — listing, searching, creating, updating and the
optimistic delete — plus the note card and editor components.

Added in PR #10 (dashboard) and PR #11 (editor).
