# `components/`

Reusable presentational components — buttons, inputs, dialogs, skeleton
loaders, empty states.

Components here take props and render. They do not fetch data, and they do not
import from `features/`.
