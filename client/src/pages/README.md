# `pages/`

One component per route: `LoginPage`, `RegisterPage`, `DashboardPage`,
`NoteEditorPage`, `ProfilePage`.

Pages compose feature components and handle layout. Business logic and data
fetching live in `features/`.
