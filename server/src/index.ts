/**
 * Server entry point.
 *
 * This file is intentionally a placeholder. The Express application, Pino
 * request logging, environment validation and the global error handler are
 * introduced in PR #2 (`feat/server-core`).
 */

export const APP_NAME = 'notes-app-server';
